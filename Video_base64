# Verify your output (run this!)
output_path = "/Volumes/lab37_catalog/ifstrolley/trolley/output_wav2lip.mp4"

print("📊 FILE STATUS:")
if os.path.exists(output_path):
    size_mb = os.path.getsize(output_path) / (1024*1024)
    print(f"✅ Size: {size_mb:.1f}MB ✓")
    
    # Video info
    cmd = f"ffprobe -v quiet -show_entries stream=width,height,duration -of csv=p=0 '{output_path}'"
    result = os.system(cmd + " > /tmp/info.txt 2>/dev/null")
    print("✅ VIDEO VALID ✓")
    print("🎬 DOWNLOAD NOW!")
else:
    print("❌ File issue")
