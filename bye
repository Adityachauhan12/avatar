# Read the tensorrt_utils.py file
tensorrt_utils_path = "/tmp/ditto-rebuild/core/utils/tensorrt_utils.py"

with open(tensorrt_utils_path, "r") as f:
    content = f.read()

# Find and show the TRTWrapper class
import re
match = re.search(r'class TRTWrapper.*?(?=\nclass |\Z)', content, re.DOTALL)
if match:
    print("Current TRTWrapper class:")
    print(match.group(0)[:1000])
    print("\n...")
else:
    print("TRTWrapper class not found. Showing file around line 100:")
    lines = content.split('\n')
    for i, line in enumerate(lines[95:115], start=95):
        print(f"{i}: {line}")
