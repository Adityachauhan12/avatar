self.repo_dir = "/tmp/ditto-talkinghead"
if not os.path.exists(self.repo_dir):
    import git
    git.Repo.clone_from(
        "https://github.com/yerfor/Ditto.git",
        self.repo_dir,
        depth=1
    )

self.data_root = "/dbfs/FileStore/ditto_checkpoints/ditto_trt_t4"
